package com.kk.hospitalapp.repository;

import com.kk.hospitalapp.model.Xray;
import org.springframework.data.jpa.repository.JpaRepository;

public interface XrayRepository extends JpaRepository<Xray, Long> {
}
